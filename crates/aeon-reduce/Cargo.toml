[package]
name = "aeon-reduce"
version = "0.1.0"
edition = "2021"

[dependencies]
aeonil = { path = "../aeonil" }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "optimization_passes"
harness = false
